name: CI

# This workflow runs when any of the following occur:
# - On any changes
on:
    push:

jobs:
    call-test-workflow:
        runs-on: ubuntu-22.04
        steps:
            - run: |
                  ./gradlew check
